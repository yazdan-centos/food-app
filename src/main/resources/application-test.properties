spring.application.name=food-app-test

# ============================================
# Server Configuration
server.port=9092
server.forward-headers-strategy=framework

# ============================================
# CORS Configuration
app.cors.allowed-origins=http://localhost,http://localhost:80,http://localhost:3000

# ============================================
# Database Configuration (update to your test database)
spring.datasource.url=jdbc:postgresql://localhost:5432/food_app_db_test
spring.datasource.username=postgres
spring.datasource.password=change-me
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

spring.main.allow-bean-definition-overriding=true
spring.main.allow-circular-references=true

# ============================================
# JWT Configuration (use a non-production secret)
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.refresh-token-expiration=604800000
jwt.clock-skew-seconds=300
security.jwt.secret-key=${jwt.secret}
security.jwt.expiration-time=86400000

# ============================================
# Logging Configuration
logging.file.path=logs
logging.level.root=INFO
logging.level.com.mapnaom=DEBUG
