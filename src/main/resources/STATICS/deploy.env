# =============================================================================
# deploy.env — Deployment Configuration for Food App
# =============================================================================
# Place this file alongside deploy.sh
# ⚠️  NEVER commit this file to Git — it contains secrets!
# =============================================================================

# --- Server ---
IP_ADDRESS=152.228.207.89

# --- GitHub (REVOKE THIS PAT AND GENERATE A NEW ONE!) ---
GITHUB_USER=yazdan-centos
GITHUB_PAT="github_pat_REPLACE_WITH_NEW_TOKEN"

# --- Database ---
DB_NAME=food_app_db
DB_USER=postgres
DB_PASS=sgsec!1390
DB_PORT=5432
# Optional: PostgreSQL network exposure
# Listen on all interfaces and allow a specific CIDR (tighten as needed)
# DB_LISTEN_ADDRESS=0.0.0.0
# DB_ALLOWED_CIDR=203.0.113.0/24

# --- Backend (Spring Boot) ---
BACKEND_REPO_URL="https://github.com/yazdan-centos/food-app.git"
BACKEND_PORT=9091
BACKEND_DIR=/opt/app/backend

# --- Frontend (React) ---
FRONTEND_REPO_URL="https://github.com/yazdan-centos/daily-meal-web-app.git"
FRONTEND_PORT=3000
FRONTEND_DIR=/opt/app/frontend